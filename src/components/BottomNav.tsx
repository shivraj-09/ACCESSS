import { Link, useLocation } from 'react-router-dom'
import { Bot, Map, MapPinPlus, Sparkles } from 'lucide-react'
const navigation = [{ label: 'Home', path: '/dashboard', icon: Sparkles }, { label: 'Map', path: '/map', icon: Map }, { label: 'Report', path: '/report', icon: MapPinPlus }, { label: 'Chat', path: '/chat', icon: Bot }]
export default function BottomNav() { const location = useLocation(); return <nav aria-label="Main navigation" className="fixed bottom-0 left-0 right-0 z-[1200] border-t border-[#d9d3c6] bg-[#f4f1ea]/96 px-2 pb-[env(safe-area-inset-bottom)] shadow-[0_-8px_24px_rgba(24,33,29,.08)] backdrop-blur md:hidden"><div className="mx-auto flex max-w-lg items-center justify-around py-1">{navigation.map(item=>{const active=location.pathname===item.path;const Icon=item.icon;return <Link key={item.path} to={item.path} className={`relative flex min-h-16 flex-1 flex-col items-center justify-center gap-1 text-[11px] font-bold ${active?'text-[#254b3f]':'text-[#858b84] hover:text-[#254b3f]'}`} aria-current={active?'page':undefined}>{active&&<span className="absolute top-0 h-0.5 w-8 bg-[#c76b43]"/>}<span className={`flex h-9 w-9 items-center justify-center ${active?'bg-[#254b3f] text-white':'bg-[#e9e5db] text-[#727870]'}`}><Icon size={17}/></span><span>{item.label}</span></Link>})}</div></nav> }